export interface ContainerProps {
  accessToken: string;
}

export interface ITrack {
  artistName: string;
  artistLink: string;
  albumName: string;
  albumLink: string;
  trackName: string;
  albumCoverLink: string;
  playedTime: number;
}